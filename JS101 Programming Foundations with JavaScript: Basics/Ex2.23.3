// What will the following code log to the console and why? Don't run it until 
// you have tried to answer.

let myWords = ['Hello'];
myWords.push('Goodbye');

console.log(myWords);

// Solution: Will output ['Hello', 'Goodbye']. The variable myWords is
// initialised with array object and then mutated by push method with by 
// adding 'Goodbye' to end of array. The console.log then outputs the now
// mutated array ['Hello', 'Goodbye'].