// Consider the following code:

function Greeting() {}

Greeting.prototype.greet = function(message) {
  console.log(message);
};

function Hello() {}

Hello.prototype = Object.create(Greeting.prototype);

Hello.prototype.hi = function() {
  this.greet('Hello!');
};

function Goodbye() {}

Goodbye.prototype = Object.create(Greeting.prototype);

Goodbye.prototype.bye = function() {
  this.greet("Goodbye");
};

// What happens in each of the following cases? Try to answer without running
// the code.


Hello.hi();

// Solution: Will raise a `TypeError`.

// On line 29 the `hi` is called on the constructor `Hello`, however, the
// constructor does not inherit from the constructor's prototype object.

// Hence, `hi` is not in the prototype chain of the `Hello` constructor and
// will raise a `TypeError`.