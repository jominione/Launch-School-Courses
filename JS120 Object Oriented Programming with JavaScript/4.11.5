// Which of the following statements about polymorphism are true? Select all
// that apply.

// A - Duck-typing is one way to achieve polymorphism.
// B - Polymorphism is the ability of objects with different types to respond
//     to the same method invocation.
// C - Subclasses that override methods inherited from a superclass is one
//     way in which we can implement polymorphism in our applications.
// D - Inheritance is not a way to achieve polymorphism.