// Consider the following code:

function Greeting() {}

Greeting.prototype.greet = function(message) {
  console.log(message);
};

function Hello() {}

Hello.prototype = Object.create(Greeting.prototype);

Hello.prototype.hi = function() {
  this.greet('Hello!');
};

function Goodbye() {}

Goodbye.prototype = Object.create(Greeting.prototype);

Goodbye.prototype.bye = function() {
  this.greet("Goodbye");
};

// What happens in each of the following cases? Try to answer without running
// the code.

let hello = new Hello();
hello.greet();

// Solution: Will output `'undefined'` because missing arguments default to
// value of `undefined`.

// One line 29 when the `greet` method is invoked, it is a method defined
// in the `Greeting.prototype` on line 5 to 7. This method takes an argument,
// value of `message` parameter, but since `greet` is invoked without an
// argument it defaults to the value `undefined`.