// Examine the following code.

let str1 = new String("abc");
let str2 = String("abc");

console.log(str1 === str2); // => false

// While line 6 shows that the two strings are not identical, it doesn't reveal
// how the strings differ. Do the strings differ in any other way?

// A - Yes.
// B - No.
// C - Indeterminate.