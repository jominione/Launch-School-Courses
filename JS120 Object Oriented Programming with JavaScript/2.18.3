// What will the `foo` function return when we invoke it from line 8 in the
// code shown below?

function foo() {
  return this;
}

foo();

// Try to answer without running the code.

// A - The global object.
// B - `null`
// C - `undefined`
// D - `foo`