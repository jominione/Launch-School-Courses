// Consider the following code:

class Critter {}
class Snake extends Critter {}
class Rattler extends Snake {}

// Based on this code, which of the following statements are true? Choose
// all that apply.

// A - `Critter` is the super-type of `Snake` and `Rattler`.
// B - `Critter` is the sub-type of `Snake` and `Rattler`.
// C - `Rattler` is the sub-type of `Snake`, while `Snake` is the sub-type
//     of `Critter`.
// D - `Rattler` is the super-type of `Snake`, while `Snake` is the super-type
//     `Critter`.