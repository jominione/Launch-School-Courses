// Think about the code you wrote for problem #1. Is there any unnecessary
// code? Does it have duplication?

// Solution:

// The method `getDescription` is repeated in each object. However, each object
// must hold unique values for its `title` and `author` properties.