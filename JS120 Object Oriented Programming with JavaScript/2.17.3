// Given the object `xyz`, how can you view the value of its `[[Prototype]]`
// property? Without running the code, choose all of the answers that will
// work.

// A - xyz.[[Prototype]];
// A - xyz["[[Prototype]]"];
// A - xyz.getPrototypeOf();
// D - xyz[[Prototype]];
// E - Object.getPrototypeOf(xyz);