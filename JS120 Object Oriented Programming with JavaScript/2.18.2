// Consider the following short program intended for execution by Node.js:

global.foo = 5;
if (isFinite(foo)) {
  let bar = 3;
  xyz = 5;
  console.log(bar);
}

// Which of the following names are properties or methods of the global object?
// Choose all that apply.

// Hint: Don't guess.

// A - global
// B - foo
// C - isFinite
// D - bar
// E - xyz
// F - console
// G - log