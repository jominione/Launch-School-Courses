// Which of the following statements describe aspects of the global object
// in JavaScript? Select all answers that apply.

// A - The global object is only available at the top-level of your program,
//     and when you don't provide an explicit execution context.
// B - The global object serves as the implicit execution context when a
//     function is invoked with method call syntax.
// C - In Node.js, the global object is named `this`. In a browser, the global
//     object is `window`.
// D - JavaScript creates a global object when it starts running.