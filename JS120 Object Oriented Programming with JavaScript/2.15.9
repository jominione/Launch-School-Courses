// Use one of the methods we learned in this lessosn to invoke `increment`
// with an explicit context such that `foo.a` gets incremented with each
// invocation of `incrementA`.

// Solution:

let foo = {
  a: 0,
  incrementA: function() {
    let self = this;
    function increment() {
      self.a += 1;
    }

    increment();
  }
};

foo.incrementA();
foo.incrementA();
foo.incrementA();

console.log(foo.a);