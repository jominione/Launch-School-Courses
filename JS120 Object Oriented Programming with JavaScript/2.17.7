// Which of the following code snippets creates a function that computes and
// returns the sum of its arguments? You may assume that each function takes
// two arguments, both of which are numbers. Try to answer without running
// the code. Select all that apply.

// A:

function sum(number1, number2) {
  return number1 + number2;
}

// B:

const sum = function(number1, number2) {
  return number1 + number2;
};

// C:

let sum = (number1, number2) => {
  number1 + number2;
};

// D:

const sum = (number1, number2) => number1 + number2;

// E:

let sum = (number1, number2) => {
  return number1 + number2;
};