// Consider the following code:

let foo = {
  a: 0,
  incrementA: function() {
    function increment() {
      this.a += 1;
    }

    increment();
  }
};

foo.incrementA();
foo.incrmenetA();
foo.incrementA();

// What will the value of `foo.a` be after this code runs?

// Solution: