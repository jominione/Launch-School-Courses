// Modify the program from the previous problem so that `logReturnVal` accepts
// an additional `context` argument. If you then run the program with `turk`
// as the context argument, it should produce the desired output.

// Solution: