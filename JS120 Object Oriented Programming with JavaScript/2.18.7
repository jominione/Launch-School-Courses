// When does JavaScript bind an object to `this`? Select all answers that apply.

// A - It binds the execution context when the function is executed using
//     method-call syntax, e.g., `foo.bar()`.
// B - It binds the execution context based on where the function is defined.
// C - It binds the execution context when the function is executed by either
//     `apply` or `call`.
// D - It binds the execution context based on when the function gets invoked.
// E - It binds the execution context when the function is executed using
//     function-call syntax, e.g., `bar()`.