// What will the following code log to the console and why?

function Ninja() {
  this.swung = true;
}

let ninja = new Ninja();

Ninja.prototype.swingSword = function() {
  return this.swung;
};

console.log(ninja.swingSword());

// Solution: Will output `true`.

// Instances of constructor functions inherit from the constructor's prototype
// object.

// On line 9 to 10 you've defined a `swingSword` method for the prototype of
// `Ninja` constructor function.

// Hence on line 13 when you make a method call on `ninja` which is an instance
// of `Ninja`, it inherits from the constructor prototype `Ninja.prototype`,
// which then outputs `true`.