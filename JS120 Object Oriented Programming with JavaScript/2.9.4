// What built-in methods have we learned about that we can use to specify a
// function's execution context explicitly?

// Solution: `call` and `apply` methods both call a function with an explicit
// execution context.