// Why is the `Array.isArray` static method useful? Choose all answers that
// apply.

// A - The `typeof` operator returns `object` when used with an array, so
//     cannot be used to detect an array.
// B - It helps improve readiability and show intent.
// C - `Array.isArray` helps distinguish between arrays and other objects.
// D - The correct reason isn't among the answers.