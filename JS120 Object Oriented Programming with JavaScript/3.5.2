// Rewrite the following code to use object-literal syntax to generate the
// returned object:

/*
function makeObj() {
  let obj = {};
  obj.propA = 10;
  obj.probB = 20;
  return obj;
}
*/

// Solution:

function makeObj() {
  return {
    propA: 10,
    probB: 20,
  };
}