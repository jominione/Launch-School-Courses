// Which of the following statements about the JavaScript mix-in pattern are
// true? Select all that apply.

// A - The mix-in pattern copies the methods and properties of one object
//     into another.
// B - The mix-in pattern is the same as inheritance.
// C - The mix-in pattern works best when there is an "is a" relationship
//     between two objects.
// D - The mix-in pattern delegates method calls on one object to the methods
//     on another object.
// E - JavaScript doesn't support multiple inheritance, but the mix-in pattern
//     helps developers overcome this limitation and get some of the benefits
//     of multiple inheritance in an environment that only supports single
//     inheritance.