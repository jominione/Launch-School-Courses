// What will the `obj.foo` method return when we invoke it from line 9?

let obj = {
  foo() {
    return this;
  },
};

obj.foo();

// Try to answer without running the code.

// A - The global object.
// B - `null`
// C - `undefined`
// D - `obj`