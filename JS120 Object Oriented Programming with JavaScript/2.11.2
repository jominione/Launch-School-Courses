// What will the following code log to the console?

let obj = {
  message: 'JavaScript',
};

function foo() {
  console.log(this.message);
}

foo.bind(obj);

// Solution: Will log nothing because unlike `call` or `apply` does not invoke
// the calling function. Instead it returns a new function that is permanently
// bound to the context argument.