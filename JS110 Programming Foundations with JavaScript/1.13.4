// What is the return value of the following statement? Why?

let result = ['ant', 'bear', 'caterpillar'].pop().length;

// Solution: Returns 11. The .pop method mutates the caller by removing the
// last element and returns the value in this case 'caterpillar'. The length
// value of caterpillar is 11.